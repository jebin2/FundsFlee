# Web framework
fastapi>=0.115
uvicorn[standard]>=0.32
python-multipart>=0.0.12

# Config & validation
pydantic>=2.9
pydantic-settings>=2.6

# Auth — jebin's google-auth-service (OAuth code flow + sessions)
google-auth-service @ git+https://github.com/jebin2/googleauthservice.git@main#subdirectory=server
PyJWT>=2.9
httpx>=0.27

# Google APIs (Sheets / Drive / Gmail, user OAuth tokens)
google-api-python-client>=2.150
google-auth>=2.35
google-auth-httplib2>=0.2

# PDF extraction (text layer + page rasterisation, no system packages needed)
pymupdf>=1.24

# AI providers
anthropic>=0.40
google-genai>=1.0

# Background scheduling & push
apscheduler>=3.10
pywebpush>=2.0

# Dev / test
pytest>=8.3
pytest-asyncio>=0.24
